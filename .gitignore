# Dependencies
node_modules/

# Dolt database directory
AI4RA-UDM/
dolt_db/

# Temporary test database (NEVER commit this)
test_db/

# Configuration file with API token (use config.template.js as template)
docs/config.js

# notes/ directory for personal notes (if you have any)
notes/
.env

# Local planning docs (per-session, not committed)
PLAN-*.md

# Claude Code local config
.claude/
